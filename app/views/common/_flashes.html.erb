<% if flash.any? %>
	<% user_facing_flashes.each do |key, value| %>
		<div class="alert alert-dismissable alert-<%= flash_class(key) %>" data-dismiss="alert">
			<%= value %>
		</div>
	<% end %>
<% end %>